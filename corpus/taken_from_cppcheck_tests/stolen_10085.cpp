void f() {
    short x;

    switch (ab) {
        case A:
            if(c)
                do_something(x);
            break;
        case B:
        default:
            if(d)
                do_something(x);
            break;
    }
}