void neoProgramShadowRegs() {
    int i;
    Bool noProgramShadowRegs;
    if (noProgramShadowRegs) {
    } else {
        switch (nPtr->NeoPanelWidth) {
        case 1280:
            VGAwCR(0x64,0x?? );
        }
    }
}